apiVersion: 1

datasources:
  - name: Redis
    type: redis-datasource
    access: proxy
    orgId: 1
    isDefault: true
    version: 1
    url: redis:6379
    editable: true

    # url: redis://${REDIS_USERNAME}:${REDIS_PASSWORD}@redis:${REDIS_PORT}
